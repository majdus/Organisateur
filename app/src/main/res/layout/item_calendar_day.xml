<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackgroundBorderless"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center"
    android:orientation="vertical"
    android:paddingVertical="1dp">

    <TextView
        android:id="@+id/day"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:gravity="center"
        android:includeFontPadding="false"
        android:textColor="@color/text_primary"
        android:textSize="14sp"
        tools:text="14" />

    <!-- Correspondance grégorienne, sous le quantième. Hors du disque de sélection, qui ne
         porte que le jour hijri. Masquée en grégorien: la case garde alors sa taille actuelle. -->
    <TextView
        android:id="@+id/gregorianDay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="-4dp"
        android:includeFontPadding="false"
        android:textColor="@color/text_tertiary"
        android:textSize="8sp"
        android:visibility="gone"
        tools:text="5/08"
        tools:visibility="visible" />

    <!-- Jusqu'à trois pastilles, aux couleurs des événements du jour: sur une grille compacte,
         c'est ce qui distingue une journée de travail d'une journée personnelle sans avoir à
         l'ouvrir. Le détail se lit dans la liste, juste en dessous. -->
    <LinearLayout
        android:id="@+id/dots"
        android:layout_width="wrap_content"
        android:layout_height="5dp"
        android:layout_marginTop="2dp"
        android:orientation="horizontal">

        <View
            android:id="@+id/dot"
            android:layout_width="5dp"
            android:layout_height="5dp"
            android:background="@drawable/bg_accent_circle"
            android:backgroundTint="@color/accent_calendar" />

        <View
            android:id="@+id/dot2"
            android:layout_width="5dp"
            android:layout_height="5dp"
            android:layout_marginStart="2dp"
            android:background="@drawable/bg_accent_circle"
            android:backgroundTint="@color/accent_calendar" />

        <View
            android:id="@+id/dot3"
            android:layout_width="5dp"
            android:layout_height="5dp"
            android:layout_marginStart="2dp"
            android:background="@drawable/bg_accent_circle"
            android:backgroundTint="@color/accent_calendar" />
    </LinearLayout>
</LinearLayout>
